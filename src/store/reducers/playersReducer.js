const initState = {
    players: []
};

const playersReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default playersReducer